# Review Questions

1. What is the difference between running config and startup config in this simulator?
2. Why does `configure terminal` require privileged exec mode first?
3. Which prompt suffix tells you that interface-specific changes will apply to the selected interface?
4. What does `write memory` change in the SQLite-backed state model?
5. Why does `POST /inject-drift` make the simulator report dirty state?
6. Which workflows are intentionally idempotent in this repo?
7. Why are the hostname, IPs, serial number, and users synthetic instead of realistic customer values?
8. How does the Windows PowerShell entrypoint stay compatible with a host that lacks Python?
9. What evidence would you inspect to confirm an SSH login happened successfully?
10. What is the rollback guidance if you accidentally save an unwanted configuration?

